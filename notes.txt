SERVER STARTUP STEPS
1. npm init -y
npm i dotenv express nodemon bcrypt sequelize mysql2
npm i jsonwebtoken
npm i nodemon
    "start": "nodemon src/server.js" in json underneath script/test
    npm run start
    Will run scripts in json

2. .gitignore with: 
.env
node_modules

3. src folder with server.js
require dotenv config
require express

4. create repo on GitHub, copy+paste commands and push initial setup

5. server:
require("dotenv").config();
const express = require("express");
const port = process.env.PORT || 5001;

const app = express();
app.use(express.json());

6. 

Switch and make new branch
git switch -c branchname

